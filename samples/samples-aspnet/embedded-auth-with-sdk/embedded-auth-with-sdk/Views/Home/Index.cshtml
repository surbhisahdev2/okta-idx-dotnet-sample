@using Microsoft.AspNet.Identity
@model IEnumerable<System.Security.Claims.Claim>

@{
    ViewBag.Title = "Home Page";
}

@if (!Request.IsAuthenticated)
{
    <div style="background-color: #ada;" class="jumbotron">
        <h1>Welcome! Create and update your documents with ONE click! </h1>
    </div>

    <div class="row">
        <div class="col-md-12">
            <!-- <h2>Login to your application below</h2> -->
     <p>
         We have made it easy for you to add your notes for each project. You will see :
         <ul>

     <li>
         <p>Find an existing project   <!--Sign Up-->
            <p />
            </li>
            <li>
                <p>
                    Add a new project <!---Password Recovery-->
                <p />
            </li>
            <li>
                <p> Use case <!--Sign Out -->
                <p />
            </li>
            </ul>
            </p>
            <!--      <p>
              To learn more about enabling advanced authentication use cases in this application check out this guide. To get started, click a button below.
          <p /> -->
     <div>

         @Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { @class = "btn btn-info", @id = "RegisterBtn" })
         @Html.ActionLink("Sign In", "Login", "Account", routeValues: null, htmlAttributes: new { @class = "btn btn-primary", @id = "LoginBtn" })
         @Html.ActionLink("Project Details", "Project", "Account", routeValues: null, htmlAttributes: new { @class = "btn btn-project", @id = "ProjectBtn" })
     </div>
        </div>
        </div>
    }
    else
    {
        @Html.Partial("_ProfilePartial", Model)
    }
